Проведи код-ревью для $ARGUMENTS.

Если аргумент — путь к файлу или папке, проверь его. Если аргумент — описание (например "последние изменения"), найди релевантные файлы через `git diff` или `git log`.

## Чеклист ревью

### Архитектура и SOLID
- [ ] Single Responsibility: один класс = одна ответственность. Кубит не делает работу сервиса, виджет не содержит бизнес-логику
- [ ] Dependency Inversion: кубиты зависят от абстракций сервисов, не от реализаций
- [ ] Виджеты не импортируют сервисы напрямую — только кубиты/state
- [ ] Нет God-классов (>200 строк — сигнал к разделению)

### KISS и YAGNI
- [ ] Нет переусложнённых абстракций (нужны ли все эти слои?)
- [ ] Нет кода "на будущее" — неиспользуемых параметров, методов, классов
- [ ] Простое решение выбрано вместо "умного"

### DRY
- [ ] Нет дублирования логики (но не преждевременная абстракция — правило трёх)
- [ ] Общие виджеты в `core/widgets/`, общие модели в `core/models/`

### Dart/Flutter качество
- [ ] `final` по умолчанию, `const` конструкторы
- [ ] Нет `dynamic`, `!`, `late` без причины, `as` без `is`
- [ ] Функции до ~30 строк, параметров до 3-4
- [ ] Отдельные виджет-классы вместо `_build` методов
- [ ] Импорты через `package:driver/...`, правильный порядок

### State Management
- [ ] State — freezed с status enum (не boolean флаги)
- [ ] Все состояния обработаны в UI: loading, success/loaded, error, empty
- [ ] `copyWith()` для переходов, нет мутации

### Обработка ошибок
- [ ] Нет пустых `catch` блоков
- [ ] Проверка connectivity перед API-вызовами
- [ ] Типизированные исключения через `mapDioException()`
- [ ] UI показывает ошибки через `showErrorSnackBar()`

### Безопасность
- [ ] Нет секретов/ключей в коде
- [ ] Нет `print()` с чувствительными данными
- [ ] Валидация входных данных

### Производительность
- [ ] `const` виджеты, `ListView.builder` для списков
- [ ] `CachedNetworkImage` для сетевых изображений
- [ ] Нет тяжёлых операций в `build()`
- [ ] `dispose()` закрывает подписки/контроллеры

## Формат ответа

Для каждой найденной проблемы:
```
🔴/🟡/🟢 [Серьёзность] Файл:строка — Описание проблемы
  Было: <текущий код>
  Стало: <предлагаемое исправление>
```

В конце — общая оценка и приоритизированный список исправлений.
