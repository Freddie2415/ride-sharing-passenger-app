Проанализируй и отрефактори $ARGUMENTS.

Если аргумент — путь к файлу/папке, работай с ним. Если аргумент — описание (например "auth feature"), найди релевантные файлы.

## Что искать

### Критичное (исправить обязательно)
- 🔴 Нарушение Dependency Inversion: кубит импортирует Dio/HTTP напрямую
- 🔴 Виджет содержит бизнес-логику или вызовы API
- 🔴 Пустые `catch` блоки или проглоченные ошибки
- 🔴 Секреты/ключи в коде
- 🔴 `dynamic` без комментария, `!` без необходимости
- 🔴 Мутация state напрямую (без `copyWith`)

### Важное (исправить при возможности)
- 🟡 God-класс >200 строк — разбить
- 🟡 Функция >30 строк — разбить
- 🟡 `_build` методы вместо отдельных виджетов
- 🟡 Отсутствие `const` конструкторов
- 🟡 Дублирование кода (>3 повторений)
- 🟡 Хардкод строк, цветов, размеров
- 🟡 Не обработаны все состояния в UI (loading/error/empty)
- 🟡 Отсутствие проверки connectivity перед API-вызовами

### Улучшения (nice to have)
- 🟢 Можно использовать extension method
- 🟢 Можно упростить выражение
- 🟢 Можно добавить `BlocSelector` вместо полной подписки
- 🟢 Можно добавить `const`/`final` где пропущено

## Процесс

1. **Анализ** — прочитай код, составь список проблем
2. **Приоритизация** — 🔴 сначала, потом 🟡, потом 🟢
3. **Рефакторинг** — исправляй по одной проблеме за раз
4. **Проверка** — после каждого исправления:
   - `dart analyze` (0 issues)
   - `flutter test` (если есть тесты для этого кода)
5. **Отчёт** — покажи что было изменено и почему

## Формат отчёта
Для каждого изменения:
```
[🔴/🟡/🟢] Принцип: SOLID/DRY/KISS/YAGNI
Файл: path/to/file.dart
Проблема: описание
Решение: что сделано
```
