Проведи полный анализ $ARGUMENTS по чеклисту качества.

## Анализ структуры
- [ ] Файлы организованы по конвенции: `cubit/`, `screens/`, `widgets/`
- [ ] Именование файлов в `snake_case`, классов в `PascalCase`
- [ ] Нет файлов >200 строк без причины
- [ ] Нет циклических зависимостей

## Анализ архитектуры
- [ ] Cubit зависит от абстракции сервиса (Dependency Inversion)
- [ ] Cubit помечен `@injectable`
- [ ] State — freezed `abstract class` с status enum
- [ ] Виджеты не импортируют сервисы
- [ ] Навигация через `go_router` (context.go/push), не из кубита

## Анализ качества кода
- [ ] `const` конструкторы везде где возможно
- [ ] `final` по умолчанию
- [ ] Нет `dynamic`, `!`, `late`, `as` без проверки
- [ ] Функции короткие (~30 строк)
- [ ] Отдельные виджет-классы, не `_build` методы
- [ ] Импорты через `package:driver/...`

## Анализ обработки ошибок
- [ ] Проверка connectivity перед API
- [ ] Типизированные исключения
- [ ] Нет пустых catch
- [ ] UI обрабатывает все статусы: initial, loading, loaded, error

## Анализ тестов
- [ ] Кубит покрыт тестами
- [ ] Модели покрыты тестами (fromJson/toJson)
- [ ] Тестируются: happy path, error, edge cases
- [ ] Фикстуры в `test/helpers/`

## Анализ производительности
- [ ] `const` виджеты
- [ ] `ListView.builder` для списков
- [ ] `CachedNetworkImage` для изображений
- [ ] `dispose()` закрывает подписки
- [ ] Нет тяжёлых операций в `build()`

## Формат ответа

### Оценка: X/10

### Критичные проблемы (исправить сейчас)
...

### Рекомендации (исправить при возможности)
...

### Хорошие практики (что уже сделано правильно)
...
